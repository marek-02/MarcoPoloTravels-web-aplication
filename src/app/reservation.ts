export interface Reservation {
    travelKey: string
    key: string
    name: string
    destination: string
    startDate: string
    endDate: string
    price: number
    seats: number
}
